package org.example.strategyDP;

public class CarPathCalculator implements PathCalculator{
    @Override
    public String findPath(String from, String to) {
        // Algo to find path for a car
        return "Path for Car from: " + from + " to: " + to;
    }
}
